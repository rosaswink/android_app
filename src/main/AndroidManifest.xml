<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rosa.japanese101">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuActivity" />
        <activity android:name=".hiraganaMenuActivity" />
        <activity android:name=".aHiragana1" />
        <activity android:name=".kaHiragana" />
        <activity android:name=".saHiragana" />
        <activity android:name=".taHiragana" />
        <activity android:name=".ckPtOne" />
        <activity android:name=".ckPtTwo" />
        <activity android:name=".ckPtThree" />
        <activity android:name=".result" />
        <activity android:name=".moreActivity"></activity>
    </application>

</manifest>